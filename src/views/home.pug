extends base.pug
include mixins/video

block content 
    div.visual
        h2.home_title Welcome here you will see the trending vedios
    div.video
        each video in videos
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
        else
            div Sorry noting found.